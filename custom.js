$('.owl-carousel').owlCarousel({
    loop: true, //Loop the images
    nav: true, //Display Next and Prev Buttons
    items: 1, //No of images to display at a time
    autoplay: true, //Auto play slides on load
    autoplayTimeout: 5000, //No of seconds for each slide
    autoplayHoverPause: false, //Stop slide show on mouse hover
});